{
  "name": "aionui-plugin-web-search",
  "version": "1.0.0",
  "description": "AionUi plugin that adds web search capabilities — works with Claude Code, Gemini, Codex, and all ACP agents",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "aionui": {
    "pluginVersion": "1.0",
    "displayName": "Web Search",
    "description": "Search the web from any AI agent. Adds a web_search tool and a skill for search best practices.",
    "icon": "./assets/icon.png",
    "category": "ai-tools",
    "minHostVersion": "1.7.0",
    "skills": [
      {
        "name": "web-search",
        "description": "Best practices for web searching: query formulation, source evaluation, and result synthesis"
      }
    ],
    "tools": [
      {
        "name": "web_search",
        "description": "Search the web and return relevant results"
      },
      {
        "name": "web_fetch",
        "description": "Fetch and extract text content from a URL"
      }
    ],
    "mcpServers": [
      {
        "name": "web-search-mcp",
        "description": "MCP server providing web search and fetch tools",
        "transport": "stdio"
      }
    ],
    "permissions": [
      "network:fetch",
      "mcp:server"
    ],
    "settings": {
      "searchEngine": {
        "type": "select",
        "label": "Search Engine",
        "description": "Which search engine to use",
        "default": "google",
        "options": [
          { "label": "Google", "value": "google" },
          { "label": "Bing", "value": "bing" },
          { "label": "DuckDuckGo", "value": "duckduckgo" }
        ]
      },
      "maxResults": {
        "type": "number",
        "label": "Max Results",
        "description": "Maximum number of search results to return",
        "default": 5,
        "validation": { "min": 1, "max": 20 }
      },
      "apiKey": {
        "type": "string",
        "label": "API Key",
        "description": "Search API key (optional for DuckDuckGo)",
        "secret": true
      }
    }
  },
  "keywords": ["aionui", "plugin", "web-search"],
  "license": "MIT"
}
